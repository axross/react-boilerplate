import { storiesOf } from "@storybook/react";
import * as React from "react";
import LoadingSpinner from "../LoadingSpinner";

storiesOf("Common/LoadingSpinner", module).add("default", () => (
  <LoadingSpinner />
));
